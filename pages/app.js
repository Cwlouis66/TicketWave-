import '../styles/globals.css'

//INTERNAL IMPORT
import {Title} from '../components/Component_index';

const MyApp = ({ Component, pageProps }) => {
    <div>
        MyApp
    </div>

  return <Component {...pageProps} />
}

export default MyApp;